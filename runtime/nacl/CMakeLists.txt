INCLUDE_DIRECTORIES("${NACL_SDK_ROOT}/include")
FIND_LIBRARY(PPAPICPPLIB NAMES ppapi_cpp PATHS /)
FIND_LIBRARY(PPAPILIB NAMES ppapi)
FIND_LIBRARY(NACLIOLIB NAMES nacl_io PATHS /)
FIND_LIBRARY(PPAPIGLES2 NAMES ppapi_gles2 PATHS /)
FILE(GLOB sources "*.cpp") 
FILE(GLOB headers "*.h")
ADD_EXECUTABLE(naclmodule ${sources} ${headers})
TARGET_LINK_LIBRARIES(naclmodule diagphysics diaggeometry diagutility ${JSONLIBS} ${POLY2TRILIBS} ${LIQUIDFUN_LIBRARIES}  ${PPAPIGLES2} ${PPAPICPPLIB} ${PPAPILIB} ${NACLIOLIB})